enum ThemeName {
  standardLight
  standardDark
}

enum FontSize {
  small
  normal
  medium
  large
}

enum LocaleName {
  ru_RU
}

type LocalStore {
  theme: ThemeName!
  fontSize: FontSize!
  locale: LocaleName!
}

extend type Query {
  localStore: LocalStore!
}
